


<h1>Eat a burger!</h1>
<div class="row">

<h2>Burgers that need be eaten!</h2>

<ul class="col-md-6">
  {{#each burger}}
  {{#unless devoured}}
    <li ><h3>{{burger_name}}</h3>
      <button class="devour" data-id="{{id}}">Devour</button>
    </li>
  {{/unless}}
  {{/each}}
</ul>
</div>
<div class="col-md-6">
<h2>Burgers that have been devoured!</h2>

<ul >
  {{#each burger}}
  {{#if devoured}}
      <li><h3>{{burger_name}}</h3></li>
  {{/if}}
  {{/each}}
</ul>
</div>
<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>
</div>

  <button type="submit">Add Burger</button>
</form>

{{!-- 

SELECT DATE_FORMAT(date, "Created a burger at <strong>%h:%m</strong> on <strong>%M %e</strong>" ) FROM burgers;
 --}}